import { ConfigInterface, ThemeInterface } from "@kyma-project/asyncapi-react";

interface AsyncApiProps {
  config?: ConfigInterface;
  theme?: ThemeInterface;
}
export { AsyncApiProps, ConfigInterface, ThemeInterface };
